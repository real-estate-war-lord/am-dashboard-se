{
  "name": "am-se-listings-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies Swedish rental listing sources for the AM Dashboard. Stores nothing.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "license": "MIT"
}
