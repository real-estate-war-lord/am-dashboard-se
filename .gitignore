# Regenerable data — never committed
data/raw/*.jsonl.gz
data/raw/_console.txt
data/geo/raw/
# OSM land polygons (1.3 GB) and the mask derived from them (396 MB):
# scripts/clip_geo.py rebuilds the mask, build_geo.py clips with it.

# Kept: data/raw/*.meta.json, *.state.json, _fetch_log.txt, _discovery.json
# (provenance — the indicator registry is built from the metadata, and the
#  state files record which geography vintage and period window each pull used)

# Secrets and local noise
.env
.DS_Store
__pycache__/
*.pyc

# Build artefacts that are not the deliverable
dist/fixture.html
tests/fixture_*.json

# Kept: data/external/raw/ — Boverket and Kronofogden source files are small
#        and the licences want the provenance kept with the derived CSV.

# Never public
data/processed/portfolio.json
data/raw/bra/
data/raw/polisen/
